# MCP Python SDK
mcp>=1.0.0

# SSE transport 依赖（如果 mcp SDK 没自动带）
# starlette>=0.35
# uvicorn>=0.27

# aiohttp 备用（如果不用 starlette）
aiohttp>=3.9.0

# 环境变量
python-dotenv>=1.0.1

# 类型 / 校验
pydantic>=2.5.0

# 装这些必须到 Editor 内置的 Python runtime，不是系统 Python：
#   <repo>/python/runtime/python-3.12-*/python/python(.exe) -m pip install -r ...
